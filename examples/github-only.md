# GitHub-only README badge row

```bash
badgekit row ci release stars license node \
  --owner topdaily-dev \
  --repo badgekit
```

For open-source repos that are not published to npm.

## With custom CI workflow

```bash
badgekit row ci --owner topdaily-dev --repo badgekit --workflow ci.yml
```
